import { Cards } from 'nextra/components'
import {ArrowDownToDot,ArrowUpFromDot,HardDriveDownload,Blocks} from "lucide-react"

# 🧩 Fondamentaux

Webpack est un outil de regroupement de modules JavaScript open source. Il résout les modules avec une syntaxe similaire à celle de Node.js et les regroupe dans des paquets (bundles) pour une utilisation dans le navigateur.

Les concepts de base de Webpack comprennent les entry points, output, loaders, et plugins. 

```javascript copy
// Importons le module 'path' pour travailler avec les chemins de fichiers
const path = require('path');

// Exportons notre configuration Webpack
module.exports = {
 // Définissons le point d'entrée de notre application
 entry: './src/index.js',

 // Définissons où Webpack doit écrire le bundle final
 output: {
   filename: 'bundle.js',
   path: path.resolve(__dirname, 'dist'),
 },

 // Ici, nous pouvons ajouter des règles pour les loaders
 module: {
   rules: [
     // Par exemple, nous pouvons ajouter un loader pour les fichiers CSS
     {
       test: /\.css$/,
       use: ['style-loader', 'css-loader'],
     },
   ],
 },

 // Et nous pouvons ajouter des plugins ici
 plugins: [],
};
```

Comprendre ces concepts est essentiel pour configurer correctement Webpack. Voici une explication de chacun de ces éléments :

<Cards>
  <Cards.Card icon={<ArrowDownToDot />} title="Entry points" href="/webpack/fondamentaux/entry_points" />
  <Cards.Card icon={<ArrowUpFromDot />} title="Output" href="/webpack/fondamentaux/output" />
  <Cards.Card icon={<HardDriveDownload />} title="Loaders" href="/webpack/fondamentaux/loaders" />
  <Cards.Card icon={<Blocks />} title="Plugins" href="/webpack/fondamentaux/plugins" />
</Cards>